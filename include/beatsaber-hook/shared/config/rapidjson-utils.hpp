#pragma once

// BeatSaver++ 0.2.3 still includes the beatsaber-hook 7 path. The implementation
// moved unchanged in beatsaber-hook 8, so keep the dependency source untouched
// and forward its legacy include to the current header.
#include "beatsaber-hook/shared/rapidjson.hpp"
